


USE DATABASE_UCB




---Agregación

/*
DROP TABLE CLIENTE
DROP TABLE EMPRESA
*/

CREATE TABLE EMPRESA(	
	ID INT NOT NULL PRIMARY KEY, 
	NOMBRE VARCHAR(100) NOT NULL
)

CREATE TABLE CLIENTE(	
	ID INT NOT NULL PRIMARY KEY, 
	NOMBRE VARCHAR(100) NOT NULL,
	ID_EMPRESA INT NULL, ---Uso de la Agregación
	FOREIGN KEY (ID_EMPRESA) REFERENCES EMPRESA(ID)
)

INSERT INTO EMPRESA VALUES
(1, 'Empresa 1'),
(2, 'Empresa 2');

INSERT INTO CLIENTE VALUES(1, 'Cliente 1', 1);

INSERT INTO CLIENTE VALUES(2, 'Cliente 2', null);
	
SELECT * FROM EMPRESA
SELECT * FROM CLIENTE






















---Composición

/*
DROP TABLE CLIENTE
DROP TABLE EMPRESA
*/

CREATE TABLE EMPRESA(	
	ID INT NOT NULL PRIMARY KEY, 
	NOMBRE VARCHAR(100) NOT NULL
)

CREATE TABLE EMPLEADO(	
	ID INT NOT NULL PRIMARY KEY, 
	NOMBRE VARCHAR(100) NOT NULL,
	ID_EMPRESA INT NOT NULL, ---Uso de la Composición
	FOREIGN KEY (ID_EMPRESA) REFERENCES EMPRESA(ID)
)

INSERT INTO EMPRESA VALUES
(1, 'Empresa 1'),
(2, 'Empresa 2');

INSERT INTO EMPLEADO VALUES (1, 'Empleado 1', 1);
INSERT INTO EMPLEADO VALUES (2, 'Empleado 2', 2);
	
SELECT * FROM EMPRESA
SELECT * FROM EMPLEADO